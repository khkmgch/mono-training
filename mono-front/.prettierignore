# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Tool-managed (generated / configured by inlang/paraglide-js)
src/lib/paraglide/
project.inlang/

# Tool-managed (json-server v1 rewrites this on every PUT/PATCH/POST/DELETE,
# so enforcing prettier formatting is futile)
db.json
